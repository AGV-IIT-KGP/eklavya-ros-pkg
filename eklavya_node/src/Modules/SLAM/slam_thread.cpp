#include "slam.h"

void *slam_thread(void *arg) {
	// ros::NodeHandle slam_node;
 //    ros::Subscriber sub = slam_node.subscribe("/robot_pose_ekf/odom", 1, slam_space::Hector::updatePose);
 //    ros::Rate loop_rate(LOOP_RATE);

 //    ROS_INFO("Started SLAM thread");

 //    while (ros::ok()) {
 //        ros::spinOnce();
 //        loop_rate.sleep();
 //    }

    return NULL;
}

