#include "slam.h"

namespace slam_space {

	    // void Hector::updatePose(const geometry_msgs::PoseWithCovarianceStamped::ConstPtr _pose) {
	    //     pthread_mutex_lock(&pose_mutex);

	    //     pose.position.x = _pose->pose.pose.position.x;
	    //     pose.position.y = _pose->pose.pose.position.y;

	    //     pthread_mutex_unlock(&pose_mutex);
	    // }
}

